# Secrets / local config
.env
.env.local

# PocketBase runtime data + local binary (schema lives in pb_migrations/)
pocketbase/pb_data/
pocketbase/pocketbase
pocketbase/pocketbase.exe
pocketbase/*.zip

# Node
node_modules/

# Build artifacts (distribute via GitHub Releases, not the repo)
*.apk

# Script secrets / data (keep the *.example.* templates)
scripts/config.json
scripts/numbers.txt

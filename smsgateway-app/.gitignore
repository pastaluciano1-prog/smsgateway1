node_modules/
.expo/
dist/
# Only the app-level native projects (regenerated by `expo prebuild`).
# Anchored with a leading slash so the native module's own modules/**/android
# SOURCE is NOT ignored — that's real code that must be committed.
/android/
/ios/
# Native module Gradle build output (keep the source, drop generated build/)
modules/**/android/build/
modules/**/android/.cxx/
npm-debug.*
*.orig.*
.DS_Store

# Release signing — keep private, never commit
release.keystore
*.keystore
